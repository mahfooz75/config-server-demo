spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mahfooz75/external-config.git
          default-label: master
server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"

